generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["taxonomy", "taxonomy_audit"]
}

model Node {
  id String @id
  @@schema("taxonomy")
}
